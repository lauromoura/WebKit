{
    "domain": "BidiBrowser",
    "exposedAs": "browser",
    "description": "The browser module contains commands for managing the remote end browser process.",
    "condition": "ENABLE(WEBDRIVER_BIDI)",
    "types": [
        {
            "id": "UserContext",
            "type": "string",
            "description": "Unique string identifying an user context"
        },
        {
            "id": "ClientWindow",
            "type": "string",
            "description": "Unique string to identify a client window"
        }
    ],
    "commands": [
        {
            "name": "close",
            "description": "Terminates the WebDriver session to which it is sent."
        }
    ]
}
